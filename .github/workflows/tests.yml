name: Backend Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # package.json declares engines.node >=20; Node 18 lacks the global
        # File class that cheerio's undici dependency requires at load time.
        node-version: [20.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Run tests
        env:
          JWT_SECRET: test-jwt-secret-for-ci
          NODE_ENV: test
          SUPABASE_URL: https://test.supabase.co
          SUPABASE_SERVICE_ROLE_KEY: test-key
          ALTTEXT_OPENAI_API_KEY: sk-test
          SEO_META_OPENAI_API_KEY: sk-test
          STRIPE_SECRET_KEY: sk_test_key
          RESEND_API_KEY: re_test_key
        run: npm test
        timeout-minutes: 10
